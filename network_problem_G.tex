\documentclass[11pt,a4paper]{article}

\usepackage{amsmath,amssymb}
\usepackage{enumitem}
\usepackage{geometry}
\geometry{margin=2.5cm}

\title{Network analysis problem set}
\author{}
\date{}

\begin{document}
\maketitle

Consider the undirected network $G = (V, E)$ with $N = 15$ nodes described by the following set of links:
\[
  E = \{(1, 2), (1, 3), (2, 4), (2, 5), (3, 6), (3, 7), (4, 8), (4, 9), (5, 10), (5, 11), (6, 12), (6, 13), (7, 14), (7, 15)\}.
\]

\begin{enumerate}[label=\textbf{(\alph*)}, leftmargin=*, itemsep=1.25em]

\item \textbf{[7 points]} Calculate the number of links $L$ in the network, and draw the network. Is the network connected? Which type of network is this? (Explain your answer).

\item \textbf{[7 points]} Write down the degree sequence, the degree distribution and the average degree of the network $G$.

\item \textbf{[6 points]} Compute the diameter $D$ of the network $G$, and list all the pairs of nodes which are at distance $D$ (Hint: it is not necessary to compute all the node distances to work out the diameter of the graph).

\item \textbf{[11 points]} Evaluate the betweenness centrality for all the nodes in the network $G$. Use the following definition of betweenness centrality of a node $i$:
\[
  b_i = \sum_{r=1}^{N} \sum_{s=1}^{N} \frac{n_{rs}^i}{n_{rs}}
\]
where $n_{rs}$ is the number of shortest paths between node $r$ and node $s$, while $n_{rs}^i$ is the number of shortest paths between node $r$ and node $s$ passing through node $i$.
\textit{Hint:} (Notice that, according to this definition, paths starting from node $i$ or ending in node $j$ are also counted).

\item \textbf{[9 points]} Which are the nodes with the largest possible values of betweenness centrality in the network, and which are those with the largest values of closeness centrality and of degree centrality?

\item \textbf{[10 points]} Suppose you can add a link to connect two nodes of the network $G$ to obtain another network $G'$. List one link choice that will create a triangle in the network $G'$, one link choice that will create a cycle of length $4$, and one link choice that will create a cycle of length $5$. How many are all the possible link choices that will guarantee that the network $G'$ has a cycle of length $\ell = 7$?

\end{enumerate}

\end{document}
